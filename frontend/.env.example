# Base URL of the HSR Club Dine API. For testing the QR flow on a real phone,
# set this to your machine's LAN IP (e.g. http://192.168.1.20:8000) so the
# scanned menu can reach the backend.
VITE_API_BASE=http://localhost:8000
